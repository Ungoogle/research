# Let's see if we were affected by Sunburst.
# https://github.com/Ungoogle
#
VAR data = GET(URLs.TxT);
f(Find);
eval(*|data());
return(Find());
Alert("See root folder for results page.");
document.write(.\Result-of-Log-SCAN.html);
End;
